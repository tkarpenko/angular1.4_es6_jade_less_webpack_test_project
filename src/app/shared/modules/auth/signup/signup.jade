.modal-content.login
  form#signup-form.form(ng-submit="signup.submit()" novalidate)
    button.modal-fb_acc_btn(type="button")
      i.icon.icon-facebook 
      | Enter with Facebook

    .form-fieldset(d-form-field-error data-fieldset="email")
      .form-tip
      .form-label email
      .form-error
      .form-input
        //- .icon.icon-heart
        .form-input-text
          input(
            name="email"
            ng-model="signup.email"
            ng-pattern="signup.patterns.email"
            required
            type="email" 
            value=""
            placeholder="Your email here")
        //- .form-input-option h

    .form-fieldset(d-form-field-error data-fieldset="password")
      .form-label password
      .form-error
      .form-input
        .form-input-text
          input(
            name="password"
            ng-model="signup.password"
            ng-pattern="signup.patterns.password"
            required
            type="password" 
            value=""
            placeholder="Your password here")

    .form-fieldset
      label.form-checkbox
        input(
          name="agree"
          ng-model="signup.agree"
          type="checkbox"
          )
        | Share content providers in Dronescom data of my registration. This information can be used for marketing purposes.

    .form-fieldset
      button.form-submit(type="submit" name="submit")
        i.icon.icon-spin
        span.form-submit_text Create account
